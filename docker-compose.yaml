version: '3.8'

services:
  backend:
    build: ./backend
    command: python manage.py run -h 0.0.0.0
    volumes:
      - ./backend/:/usr/src/app/
    ports:
      - 5000:5000
    env_file:
      - ./.env